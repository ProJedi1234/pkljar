// pkljar — Pkl environments: generate, inject, sync.
// See README.md for the design. The converter (`generate` / `run`) is the first milestone.

@main
struct Pkljar {
    static func main() {
        print("pkljar")
    }
}
